# O2O Demand Forecasting Solution 전체 리뷰

- 리뷰 일자: 2026-08-01
- 리뷰 대상: 현재 `main` 브랜치 및 작업 트리 변경분
- 중점 영역: 데이터 수집, 수요 점수 파이프라인, 시계열 ML, Flask API, 대시보드 UI, 재현성

## 2026-08-02 반영 결과

초기 리뷰의 12개 항목은 모두 코드에 반영되었다. 주요 후속 검증 결과는 다음과 같다.

- 실거래 매매·전월세 API가 `totalCount` 기준으로 마지막 페이지까지 수집된다.
- 정상 0건과 API 실패를 구분하며, 실패 호출은 재시도 후에도 실패하면 저장을 중단한다.
- 기간 갱신 시 정상 0건 지역을 포함해 해당 지역·월 기존 행을 완전히 교체한다.
- 2026년 1~6월 재수집 완료: 매매 179,904건, 전월세 357,824건.
- 정확히 1,000건인 지역·월 그룹은 매매·전월세 모두 0개로 확인됐다.
- 시계열 분석 기간은 2025년 7월~2026년 6월이며 미래 예측은 2026년 7~9월이다.
- AI 해설은 추세 방향과 기울기 숫자의 부호를 검증한 뒤 결과에 포함한다.
- 390px 모바일 화면에서 대시보드 네 페이지 모두 가로 넘침이 없다.
- 테스트 결과: `48 passed`.

아래 내용은 최초 리뷰 당시의 발견 사항을 보존한 기록이다.

## 요약

현재 프로젝트는 공공데이터 수집부터 수요 점수 산출, 시계열 모델 비교, Flask API, 대시보드 시각화까지 연결된 완성도 높은 포트폴리오 구조를 갖추고 있다.

최초 리뷰 당시에는 원천 데이터 페이지네이션 누락, `/api/collect`의 전국 결과 덮어쓰기, 시계열 평가 방식과 화면 설명의 불일치가 결과 신뢰도에 직접 영향을 주고 있었다. 해당 항목을 포함한 아래 12개 발견 사항은 2026-08-02 작업에서 모두 해결하고 재검증했다.

## 주요 발견 사항

### 1. [해결 완료][P1] 실거래가 API 페이지네이션 누락

**위치:** `src/collector.py:221-253`

수집 요청이 항상 아래 값으로 고정되어 있다.

```python
"numOfRows": 1000,
"pageNo": 1,
```

현재 수집 CSV를 검사한 결과 다음 그룹이 정확히 1,000건이었다.

- 매매: 3개 지역·월 그룹
- 전월세: 191개 지역·월 그룹

정확히 상한과 일치하는 그룹은 2페이지 이후 데이터가 잘렸을 가능성이 높다.

**영향**

- 지역별 거래건수 과소 집계
- 전월세거래건수 20% 점수 왜곡
- 시계열 월별 거래량 과소 집계
- 지역 순위와 모델 학습 결과 신뢰도 저하

**권장 수정**

- 응답의 `totalCount`를 읽어 마지막 페이지까지 반복 수집
- 페이지별 재시도와 실패 로그 추가
- 지역·월별 수집 건수 및 페이지 수를 manifest로 저장

### 2. [해결 완료][P1] `/api/collect`가 전국 결과를 서울 결과로 덮어씀

**위치:** `app.py:163-206`

`sigungu_code`가 없을 때 수집 범위가 `SEOUL_SIGUNGU_CODES`로 제한된다.

```python
if sigungu_code:
    codes = {k: v for k, v in SEOUL_SIGUNGU_CODES.items() if v == sigungu_code}
else:
    codes = SEOUL_SIGUNGU_CODES
```

이후 생성된 서울 결과를 다음 전국 결과 파일에 덮어쓴다.

```text
data/인테리어_수요점수_결과.csv
data/시도별_수요집계_요약.csv
```

현재 정상 결과는 8개 시도, 102개 시군구인데 이 API를 기본값으로 실행하면 서울 25개 구만 남을 수 있다.

별도 스크립트가 추가하는 `소상공인_인테리어업체수`도 기본 파이프라인에는 포함되지 않으므로 함께 사라진다.

**추가 위험**

- 장시간 실행되는 수집 API에 인증이 없음
- 외부 요청으로 공공 API 할당량 소진 가능
- 실행 중 기존 CSV를 직접 덮어써 부분 결과가 노출될 수 있음

**권장 수정**

- 전체 지역 코드 집합을 단일 상수로 구성
- 특정 지역 수집은 전체 결과에 upsert
- 임시 파일에 저장한 뒤 성공 시 원자적으로 교체
- 수집 API에 인증 또는 관리자 전용 실행 방식 적용
- 소상공인 업체 데이터 병합을 공식 파이프라인 단계로 통합

### 3. [해결 완료][P1] 시계열 화면이 실제 미래 예측을 제공하지 않음

**위치:** `analyze_timeseries.py:25-68`, `templates/forecast.html:211-219`

현재 스크립트는 데이터를 다음처럼 나눈다.

```python
train = monthly.iloc[:9].copy()
test = monthly.iloc[9:].copy()
```

생성되는 JSON에는 마지막 2개월의 검증 예측값만 있고, 학습 데이터 이후의 미래 월 예측값은 없다.

하지만 화면에서는 다음 달 거래량을 예측한다고 설명한다. 구현은 모델 백테스트이고 화면은 미래 예측으로 표현되어 있어 의미가 다르다.

**권장 수정**

- 현재 결과는 `모델 백테스트` 또는 `검증 구간 비교`로 명확히 표시
- 미래 예측을 제공하려면 전체 데이터로 최종 모델을 다시 학습
- `forecast_horizon`, `future_ym`, `future_predictions`를 별도로 생성
- 데이터가 충분해질 때까지 예측값보다 실험적 비교라는 점을 강조

### 4. [해결 완료][P1] LightGBM 평가 조건이 SARIMA·Prophet과 다름

**위치:** `analyze_timeseries.py:48-60`

전체 데이터로 lag 피처를 먼저 생성한 뒤 학습/검증 데이터를 분리한다.

```python
full[f"lag{lag}"] = full["y"].shift(lag)
test_feat = full_feat[full_feat["ds"] >= test["ds"].min()]
```

이 방식에서는 두 번째 검증 월을 예측할 때 첫 번째 검증 월의 실제 거래량이 lag 피처로 사용된다. 반면 SARIMA와 Prophet은 두 달을 한 번에 예측한다.

따라서 LightGBM은 rolling one-step 평가이고 다른 두 모델은 multi-step 평가여서 MAPE를 직접 비교하기 어렵다.

**권장 수정**

- 모든 모델을 동일한 rolling-origin 방식으로 평가하거나
- 모든 모델이 검증 시작 시점 정보만 사용해 같은 horizon을 예측하도록 통일
- 검증 구간을 2개월보다 길게 확보
- 시계열 cross-validation 결과의 평균과 분산을 함께 제시

### 5. [해결 완료][P1] 새 저장소에서 앱이 실행되지 않을 수 있음

**위치:** `app.py:33-67`, `.gitignore`

현재 `data/` 아래 파일은 Git에 추적되지 않는다. 그러나 앱 import 시점에 다음 경로로 SQLite 연결을 시도한다.

```python
CHAT_DB_PATH = os.path.join(os.path.dirname(__file__), "data", "chat_history.db")
init_chat_db()
```

`data` 디렉터리가 없는 새 clone에서는 `sqlite3.connect()`가 실패할 수 있다. 대시보드가 요구하는 결과 CSV와 좌표 CSV도 저장소에 없으므로 바로 실행할 수 없다.

**권장 수정**

- 앱 초기화 시 `os.makedirs(data_dir, exist_ok=True)` 실행
- 작은 데모 데이터 또는 데이터 생성 명령 제공
- 필수 파일 누락 시 친절한 상태 페이지와 준비 명령 표시
- 생성 데이터와 필수 정적 데이터를 `.gitignore`에서 구분
- 의존성 버전을 고정해 재현 가능한 설치 환경 제공

### 6. [해결 완료][P2] 노후도 기준연도가 2025년으로 고정됨

**위치:** `src/pipeline.py:6`, `src/pipeline.py:183`, `src/pipeline.py:224`

```python
REFERENCE_YEAR = 2025
```

2026년 거래 데이터에도 2025년 기준 노후도가 적용된다.

**영향**

- 15년과 20년 경계 아파트의 세그먼트 오분류
- 2026년 준공 아파트가 음수 노후도로 제거됨
- 매매와 전월세 집계 및 점수 변화

**권장 수정**

- 거래연도 또는 데이터 기준일을 사용해 노후도 계산
- 분석 기준연도를 실행 인자와 결과 메타데이터에 명시
- 14/15년, 20/21년 경계 테스트 추가

### 7. [해결 완료][P2] 데이터 미수집을 실제 0건으로 처리함

**위치:** `src/pipeline.py:403-424`, `collect_metro5.py`

5대 광역시는 대수선 데이터가 수집되지 않았지만 `대수선이력건수=0`으로 처리되고, 최종 점수에서 10% 가중치를 받는다.

이는 `관측되지 않음`과 `실제로 대수선 이력이 없음`을 동일하게 취급한다.

**영향**

- 대수선 데이터가 없는 지역이 구조적으로 불리함
- 수도권과 비수도권 점수의 직접 비교가 불공정할 수 있음

**권장 수정**

- 데이터 커버리지 컬럼 추가
- 결측 지표를 제외하고 남은 가중치를 재정규화
- 모든 대상 지역의 데이터 수집 범위를 통일
- 대시보드에 지역별 데이터 완전성 표시

### 8. [해결 완료][P2] 모바일 반응형 레이아웃 부재

**위치:** `templates/index.html:433-465` 및 다른 템플릿의 header/main

390px 모바일 viewport에서 확인한 결과:

- 실제 viewport 폭: 약 386px
- 문서 폭: 약 903px
- 가로 스크롤 발생
- 헤더 내비게이션과 본문이 오른쪽에서 잘림

현재 주요 레이아웃이 인라인 `grid-template-columns`로 고정되어 있고 모바일용 `@media` 규칙이 없다.

**권장 수정**

- 모바일에서 main grid를 1열로 변경
- KPI와 비교 입력을 1~2열로 축소
- 헤더 내비게이션을 메뉴 또는 가로 스크롤 구조로 변경
- 고정 padding과 최소 폭 조정
- 390px, 768px, 1440px viewport 회귀 테스트 추가

### 9. [해결 완료][P3] 검색어가 정규식으로 해석됨

**위치:** `app.py:229`, `app.py:237-238`

사용자 검색어를 그대로 `str.contains()`에 전달한다. 실제로 `q=[` 요청은 잘못된 정규식 오류로 HTTP 500을 반환했다.

**권장 수정**

```python
df["시군구"].str.contains(q, na=False, regex=False)
```

검색 길이 제한과 정상적인 400 응답도 추가하는 것이 좋다.

### 10. [해결 완료][P2] 개발용 Flask 서버가 외부 인터페이스에 노출됨

**위치:** `app.py:613-617`

```python
host="0.0.0.0"
debug=True
```

실행 로그에서도 외부 주소에 debugger가 활성화된 상태가 확인됐다. 로컬 포트폴리오 시연에는 편리하지만 네트워크에 연결된 환경에서는 위험하다.

**권장 수정**

- 기본값은 `127.0.0.1`, `debug=False`
- 환경변수로 개발 모드 명시
- 배포 시 WSGI 서버 사용
- 쓰기 API 인증 적용

### 11. [해결 완료][P2] AI 해설 결과를 사실 검증 없이 그대로 캐싱함

**위치:** `analyze_timeseries.py:71-127`

현재 캐시된 AI 해설은 2025년 7월부터 11월까지 거래량이 지속적으로 증가했다고 설명하지만 실제 데이터에는 감소 구간이 있다. 2026년 4월에서 5월도 감소했지만 최근 추세를 상승이라고 판단했다.

프롬프트에서 숫자를 제한해도 자연어 해석 오류는 발생할 수 있다.

**권장 수정**

- 상승/하락, 증감률, 최우수 모델 판단은 코드로 계산
- LLM에는 계산된 판정문의 문장화만 맡김
- 생성 결과에 데이터와 모순되는 표현이 있는지 검증
- AI 해설 생성 실패와 검증 실패를 구분해 저장

### 12. [해결 완료][P3] 자동화 테스트 부재

테스트 파일을 찾지 못했다. 현재 구조에서는 다음 테스트가 특히 필요하다.

- API 페이지네이션 및 재시도
- 노후도 세그먼트 경계
- 가중치 합계 및 Min-Max 정규화
- 결측 데이터 가중치 처리
- 지역 코드 매핑
- `/api/collect` 부분 업데이트와 원자적 저장
- 시계열 피처의 미래 정보 사용 방지
- Flask API 정상/오류 응답
- 모바일 viewport 가로 overflow 검사

## 최신 UI 변경 검토

현재 작업 트리에는 다음 템플릿의 라이트/다크 테마 변경이 있다.

- `templates/index.html`
- `templates/analytics.html`
- `templates/forecast.html`
- `templates/guide.html`

데스크톱에서는 테마 저장, 배경색, 텍스트 대비, 토글 아이콘이 정상적으로 동작했다. 기존 Chart.js 인스턴스도 테마 변경 시 update가 호출된다.

차트 내부의 일부 직접 지정 색상은 유지되지만 라이트·다크 테마에서 대비를 확보했고, 모바일 레이아웃은 390px 기준 네 페이지 모두 가로 넘침 없이 수정했다.

## 검증 결과

- Python `compileall` 통과
- 자동화 테스트 `48 passed`
- `/`, `/analytics`, `/guide`, `/forecast` 응답 HTTP 200
- `/health`, `/api/demand`, `/api/forecast` 응답 HTTP 200
- 잘못된 `top` 및 정규식 특수문자 검색 입력 HTTP 400/정상 빈 결과 처리
- 결과 데이터 102개 시군구, 최고 수요점수 65.73점
- 2026년 상반기 매매 179,904건, 전월세 357,824건
- 지역·월별 1,000건 절단 그룹 없음
- 390px 모바일 네 페이지 가로 overflow 없음
- SARIMA·Prophet·LightGBM 실행 및 미래 3개월 예측 생성 정상
- 데스크톱 라이트/다크 테마 전환 정상

## 수정 완료 순서

1. 실거래가·전월세 API 페이지네이션과 실패 감지 구현
2. `/api/collect`의 기간 단위 원자적 교체 적용
3. 시계열 평가 조건 통일 및 실제 미래 예측 분리
4. 데이터 디렉터리 초기화와 경량 결과 데이터 포함
5. 기준연도와 데이터 기간 동적 계산
6. 결측 지표의 가중치 재정규화 적용
7. 모바일 반응형 레이아웃 추가
8. API 입력 검증과 배포 설정 정리
9. 핵심 파이프라인 및 API 테스트 추가

## 최종 평가

프로젝트의 범위와 end-to-end 구성은 포트폴리오로서 강점이 있다. 특히 여러 공공데이터를 지역 단위로 결합하고 결과를 API와 대시보드로 제공한 점은 좋다.

초기 리뷰에서 지적한 데이터 정확성, 평가 설계, API 안정성 문제는 모두 수정됐다. 현재 시계열 데이터는 12개월로 여전히 짧으므로 계절성 해석과 장기 예측에는 한계가 있지만, 백테스트와 미래 예측의 의미를 분리하고 해당 한계를 화면과 AI 해설에 명시해 포트폴리오 결과의 신뢰성을 확보했다.
